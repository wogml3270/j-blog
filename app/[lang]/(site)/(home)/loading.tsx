import { ImmersiveHeroSkeleton } from "@/components/home/immersive-hero-skeleton";

// 공개 사이트 라우트 전환 시 공통으로 보여줄 페이지 레벨 스켈레톤이다.
export default function SiteLoading() {
  return <ImmersiveHeroSkeleton />;
}
